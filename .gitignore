node_modules
*.env
.env.local
.DS_Store
.vercel

# Native build artefacts — regenerated by Gradle/Xcode, never source.
# `android/.gradle/` appeared untracked in BOTH this mirror and the monorepo
# on 2026-08-15 (a Gradle 9.2.0 probe; android/ has no gradlew/settings.gradle
# so nothing actually built). Ignored here because this file is the root of the
# public paybridgenp/mobile-sdk mirror AND is honoured by the monorepo.
.gradle/
build/
# iOS
Pods/
*.xcworkspace/xcuserdata/
xcuserdata/
